# Hostinger SMTP (hPanel → Emails → Connect apps & devices)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=hello@densitea.in
# Mailbox password from hPanel → Emails → Mailboxes (quote if special chars)
SMTP_PASS="your-mailbox-password-here"

SMTP_FROM=hello@densitea.in
CONTACT_TO_EMAIL=hello@densitea.in
